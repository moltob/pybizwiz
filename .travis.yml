language: python
python:
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install pytest-cov coveralls  # coverage measurement
script:
  - py.test test -v --cov=bizwiz
after_success:
  - coveralls